# 报表配置能力

* 报表动态参数配置，支持动态传递参数信息。
* 针对列表的数据，通过循环块的方式实现，循环块数据设置中，不仅需要设置字段，还需要设置排序的字段
* 循环块下可以嵌套循环块
* 属性设置面板，支持传入多个属性，输出属性支持 规则处理，对数据进行合并取平均值等操作，返回数据还可以支持数据格式设定的能力，包括图片格式的展示能力。
* 针对属性可以设置数据反查的取值逻辑，反查展示数据的字段配置。
* 再y轴、x轴的条件配置上，还需要支持条件的排练方式，（）并且或者等各种自由组合的方式。
* 后续可以考虑支持图表的能力，折线图、柱状图等展示方式。